databaseChangeLog:
  - changeSet:
      id: init-changes
      author: myusername
      changes:
        - createTable:
            tableName: users
            columns:
              - column:
                  name: id
                  type: BIGINT AUTO_INCREMENT PRIMARY KEY
              - column:
                  name: username
                  type: VARCHAR(50)